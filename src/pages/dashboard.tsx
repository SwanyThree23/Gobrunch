import React from 'react';

export default function Dashboard() {
    return (
        <div className="p-6">
            <h1 className="text-3xl font-heading">Command Center Dashboard</h1>
            <p>Stats cards, revenue sparkline, recent streams.</p>
        </div>
    );
}