import React from 'react';

export const LiveBadge: React.FC = () => (
    <span className="inline-flex items-center gap-1.5 px-2.5 py-0.5 bg-red-500/20 text-red-400 text-xs font-bold rounded-full uppercase tracking-wider">
        LIVE
    </span>
);
